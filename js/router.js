define(['underscore', 'backbone'], function() {
   
    var router = Backbone.Router.extend({

    	routes : {
    		"" : "index"
    	},

    	index: function(){	
    	}

    });
   
    return router; 
});